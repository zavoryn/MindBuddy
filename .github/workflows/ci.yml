name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package
        run: python -m pip install -e ".[dev]"

      - name: Compile sources
        run: python -m compileall -q mindbuddy tests

      - name: Lint (ruff)
        run: |
          python -m pip install ruff
          python -m ruff check mindbuddy/ tests/ --select=E,F,B,I,UP --ignore=E501

      - name: Check import sorting (isort)
        run: |
          python -m pip install isort
          python -m isort --check-only --diff mindbuddy/ tests/ || true

      - name: Run packaging smoke tests
        run: python -m pytest tests/test_packaging.py -q

      - name: Run test suite
        run: python -m pytest -q
